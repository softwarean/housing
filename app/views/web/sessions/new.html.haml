.page-header
  %h1= t('.title')
= simple_form_for @session, url: session_path do |f|
  .row
    .col-md-5
      = f.input :email
      = f.input :password
      .form-group
        = f.button :submit, t('login'), class: 'btn btn-primary'
